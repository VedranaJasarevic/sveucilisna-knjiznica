
@if (Session["Email"] == null)
{



    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("Registracija", "Register", "Account", null, null)
        </li>

        <li>
            @Html.ActionLink("Prijava", "Login", "Account", null, null)
        </li>

    </ul>
}

else
{
    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink(" " +
                             Session["Email"], "Index", "Home", null, null)
        </li>

        <li>
            @Html.ActionLink("Odjava", "LogOut", "Account", null, null)
        </li>

    </ul>
}