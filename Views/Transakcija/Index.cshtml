@model IEnumerable<SVEUCILISNA_KNJIZNICA.Models.Transakcija>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DatumTransakcije)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BrojDokumenta.NazivDokumenta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Korisnik.Ime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DatumTransakcije)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BrojDokumenta.NazivDokumenta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Korisnik.Ime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TransakcijaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TransakcijaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TransakcijaID })
        </td>
    </tr>
}

</table>
